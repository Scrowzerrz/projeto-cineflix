
// Este arquivo está mantido por questões de compatibilidade
// A implementação foi refatorada para arquivos menores em src/hooks/auth/*
import { useAuth } from './auth/useAuth';
import { AuthProvider } from './auth/AuthProvider';

export { useAuth, AuthProvider };
